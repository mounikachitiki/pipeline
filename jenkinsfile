pipeline {
    agent { label 'mvn-buildagent-forjava' }
    stages {
        stage('clone') {
            steps {
               git branch: "main", url: 'https://github.com/mounikachitiki/pipeline.git'
            }
        }
        stage('build') {
            steps {
                sh 'mvn clean install'
            }
        }
        
    }
}
